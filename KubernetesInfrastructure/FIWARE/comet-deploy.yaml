apiVersion: apps/v1
kind: Deployment
metadata:
  name: comet
spec:
  selector:
    matchLabels:
      run: comet
  replicas: 1
  template:
    metadata:
      labels:
        run: comet
    spec:
      containers:
      - image: amarbajric/sth-comet:2.4
        name: comet
        env:
        - name: MAX_PAGE_SIZE
          value: "10000"
        ports:
        - containerPort: 8666
        volumeMounts:
        - mountPath: /opt/sth/config.js
          name: comet-config
          subPath: config.js
      restartPolicy: Always
      volumes:
      - name: comet-config
        configMap:
          name: comet-config
