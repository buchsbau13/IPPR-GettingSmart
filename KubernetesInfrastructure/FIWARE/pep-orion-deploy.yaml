apiVersion: apps/v1
kind: Deployment
metadata:
  name: pep-orion
spec:
  selector:
    matchLabels:
      run: pep-orion
  replicas: 1
  template:
    metadata:
      labels:
        run: pep-orion
    spec:
      containers:
      - name: pep-orion
        image: fiware/pep-proxy:7.0.1
        ports:
        - containerPort: 1026
        volumeMounts:
        - mountPath: /opt/fiware-pep-proxy/config.js
          name: pep-orion-config
          subPath: config.js
        - mountPath: /opt/fiware-pep-proxy/lib/azf.js
          name: pep-orion-config
          subPath: azf.js
      restartPolicy: Always
      volumes:
      - name: pep-orion-config
        configMap:
          name: pep-orion-config
