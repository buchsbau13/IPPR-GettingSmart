# Provide MongoDB to other Pods
apiVersion: v1
kind: Service
metadata:
  name: mongo
  namespace: {{.Values.fiware.namespace}}
  labels:
    app: mongo
spec:
  ports:
  - port: 27017
    name: fiware-mongo
  clusterIP: None
  selector:
     app: mongo
