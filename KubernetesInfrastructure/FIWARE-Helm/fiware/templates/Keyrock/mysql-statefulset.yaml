apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: mysql
spec:
  selector:
    matchLabels:
      app: mysql # has to match .spec.template.metadata.labels
  serviceName: "mysql"
  replicas: 1 # by default is 1
  template:
    metadata:
      labels:
        app: mysql # has to match .spec.selector.matchLabels
    spec:
      containers:
      - name: mysql
        image: mysql/mysql-server:{{.Values.mysql.version}}
        securityContext:
          fsGroup: 65534
        env:
        - name: MYSQL_ROOT_PASSWORD
          value: {{ .Values.mysql.root_password | quote}}
        - name: MYSQL_ROOT_HOST
          value: "%"
        ports:
        - containerPort: 3306
        volumeMounts:
        - name: mysql-pv-1
          mountPath: /var/lib/mysql
  volumeClaimTemplates:
  - metadata:
      name: mysql-pv-1
    spec:
      accessModes:
      - {{.Values.mysql.pv.accessModes}}
      storageClassName: {{ .Release.Namespace}}-mysql
      resources:
        requests:
          storage: {{.Values.mysql.pv.storage}}
