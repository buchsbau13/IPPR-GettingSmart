apiVersion: v1
kind: Service
metadata:
  name: keyrock
  labels:
    run: keyrock
spec:
  ports:
  - port: {{.Values.keyrock.port}}
    targetPort: {{.Values.keyrock.port}}
    nodePort: {{.Values.keyrock.nodePort}}
  selector:
    run: keyrock
  type: LoadBalancer
